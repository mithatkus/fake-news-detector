fastapi==0.111.0
mangum==0.17.0
uvicorn==0.29.0
pydantic==2.7.1
boto3==1.34.100
joblib==1.4.2
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2
nltk==3.8.1
# matplotlib ≥3.10 drops the manylinux2014 wheel; Amazon Linux 2 has glibc 2.26
# which satisfies manylinux2014 but NOT manylinux_2_28, causing a slow source build.
# Pin to 3.9.x to guarantee a pre-built aarch64 wheel.
matplotlib>=3.7.0,<3.10.0
scikit-image>=0.19.0,<0.25.0
lime==0.2.0.1
# Lambda (arm64/Graviton2) — tensorflow-cpu has no aarch64 wheel.
# TF 2.17 is the first release with Python 3.12 support; it ships
# manylinux2014 aarch64 wheels that work on Amazon Linux 2023 (glibc 2.34).
# The .keras model format produced by TF 2.16 is fully compatible with TF 2.17.
tensorflow==2.17.0
